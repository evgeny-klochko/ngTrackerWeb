(function () {
  'use strict';

  angular
    .module('TSC.dashboard')
    .controller('CommonCtrl', CommonCtrl);

  /* @ngInject */
  function CommonCtrl() {
    var vm = this;

    angular.extend(vm, {

    });

    angular.extend(vm, {

    });
  }
}());
