(function () {
  'use strict';

  angular
    .module('TSC.profile')
    .controller('ProfileCtrl', ProfileCtrl);

  /* @ngInject */
  function ProfileCtrl() {
    var vm = this;

    angular.extend(vm, {

    });

    angular.extend(vm, {

    });
  }
}());
